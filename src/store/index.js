import { reactive } from 'vue'

const state = {
  soundBoards: reactive([]),
  soundBoard: reactive({}),
  sounds: reactive([])
}

async function addSoundBoard(){

}

async function renameSoundBoard(){
  
}

async function deleteSoundBoard(){
  
}

async function moveSoundBoard(){
  
}